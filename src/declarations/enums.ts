export enum FetchPriority {
  HIGH = 'high',
  LOW = 'low',
  AUTO = 'auto'
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark'
}

export enum SkeletonType {
  TEXT = 'text',
  BUTTON = 'button',
  IMG = 'img'
}
